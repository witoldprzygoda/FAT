{
    // ============================================================================
    // TRIGGER/SELECTION CONFIGURATION
    // Trigger masks and event selections
    // ============================================================================
    
    // Physics trigger (PT3) - standard physics events
    "physics": {
        "mask": 4,
        "description": "Physics trigger PT3 - standard physics events"
    },
    
    // Calibration trigger
    "calibration": {
        "mask": 8,
        "description": "Calibration trigger"
    },
    
    // Pulser trigger  
    "pulser": {
        "mask": 32,
        "description": "Pulser trigger for detector timing"
    },
    
    // Combined triggers (OR)
    "physics_or_calibration": {
        "mask": 12,
        "description": "Physics OR Calibration triggers"
    }
}
